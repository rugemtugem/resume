/**
 * Custom Hooks Export Barrel
 * 
 * Centralized export for all custom hooks.
 * 
 * @example
 * ```tsx
 * import { useRipple, useMagnetic } from '@/hooks';
 * ```
 */

export { useRipple } from './use-ripple';
export { useMagnetic } from './use-magnetic';
